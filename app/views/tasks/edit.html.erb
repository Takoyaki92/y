<%= form_for(@task) do |f| %>
<%= f.label :title %>
<%= f.text_field :title %>
<%= f.label :details %>
<%= f.text_field :details %>
<%= check_box_tag :completed %>
<%= f.submit %>

<% end %>

<p><%= link_to "Go back to list", tasks_path %></p>
