<h1>Do this stuff!</h1>

<ol><% @tasks.each do |task| %>

  <li><%= link_to task.title, task_path(task) %></li>

  <%= link_to task_path(task), method: :delete, data: {
  confirm: 'You sure you wanna throw this in the dumpster?'
} do %>
    <i class="fas fa-dumpster-fire"></i>
  <% end %>
<% end %>
</ol>


<p><%= link_to "add new task", new_task_path(@task) %></p>

